---
import { Image } from 'astro:assets';
import Layout from '@/layouts/Layout.astro';
import me from '@/images/me.png';
import luna from '@/images/luna.png';
import space from '@/images/space.jpg';
import plane from '@/images/plane.jpeg';
---

<Layout title="Ash Johns | About">
  <h1>A little about <span class="text-gradient">me</span></h1>
  <div class="about">
    <div class="image">
      <Image src={me} alt="A picture of me" class="me" />
    </div>
    <!-- <div class="gallery">
      <Image src={me} alt="A picture of me" class="me" />
      <Image src={luna} alt="A picture of my cat" class="me" />
      <Image src={space} alt="Pictures I took of the Moon, Jupiter, and Saturn" class="me" />
      <Image src={plane} alt="Pictures I took of the Moon, Jupiter, and Saturn" class="me" />
    </div> -->
    <p class="about-text">
      Hi! I'm Ash. I'm a Boston based Frontend Developer. My latest focus has been on building out design systems from
      the ground up. That includes everything from developing React components, to setting up CI/CD and everything
      in-between. These experiences have made me very good at writing reusable code with a great developer experience.
      Code that abides to frontend best practices and accessibility standards. I'm always looking to apply these skills
      in new ways, so don't hesitate to reach out!
    </p>
  </div>
</Layout>

<style define:vars={{image: `url("${me.src}")`}}>
  .about {
    display: grid;
    grid-template-columns: 1fr;
    /* grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); */
    align-items: center;
    border-radius: 8px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    background: #23262d;

    & p {
      margin: 0;
      padding: 1.5rem;
    }
  }

  .gallery {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
  }

  .image {
    display: flex;
    align-self:stretch;
  }

  .me {
    display: block;
    box-sizing: border-box;
    min-width: 100px;
    width: 100%;
    height: auto;
    aspect-ratio: 1/1;
    object-fit: cover;
    object-position: center;
    /* border: 8px solid #13151a; */
    border-radius: 8px 8px 0 0;
    /* padding: 1.5rem; */
  }

  @media (min-width: 640px) {
    .about {
      grid-template-columns: 1fr 1fr;
    }

    .me {
      aspect-ratio: unset;
      border-radius: 8px 0 0 8px;
    }
  }

  @media (min-width: 768px) {
    .about {
      grid-template-columns: 1fr 2fr;
    }
  }

  @media (min-width: 1024px) {
    .about {
      border-radius: 50px 8px 8px 50px;
    }

    .image {
      position: relative;
      align-self: stretch;
    }

    .me {
      position: absolute;
      display: block;
      inset: 0;
      left: -25%;
      top: -5%;
      box-sizing: border-box;
      width: auto;
      height: 110%;
      aspect-ratio: 1/1;
      object-fit: cover;
      object-position: center;
      border: 8px solid #13151a;
      border-radius: 50%;
    }
  }
</style>
